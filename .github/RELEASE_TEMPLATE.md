## ReleaseFlow v{{version}}

### Changes
{{changelog}}

### Installation
```bash
npm install -g releaseflow
# or
npx releaseflow
```

### Usage
```bash
npx releaseflow          # Auto-detect version bump
npx releaseflow patch    # Explicit patch bump
npx releaseflow --dry-run # Preview without executing
```
